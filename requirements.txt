# Core dependencies for streamripper
av>=10.0.0
matplotlib>=3.5.0
pandas>=1.3.0

# Installation options:
# Recommended: uv pip install -e ".[dev]" (after uv venv)
# Alternative: pip install -r requirements-dev.txt
# Or use: pip install -e ".[dev]" to install from pyproject.toml